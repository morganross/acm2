# =============================================================================
# ACM2 Environment Variables
# =============================================================================
# Copy this file to /opt/acm2/acm2/.env and fill in the values.
# DO NOT commit .env with real values to git!
# =============================================================================

# Required: Encryption key for user provider API keys
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Required: Default preset configuration
SEED_PRESET_ID=86f721fc-742c-4489-9626-f148cb3d6209
SEED_VERSION=1.0.0

# Required: Shared secret for WordPress plugin authentication
ACM2_PLUGIN_SECRET=

# Optional: Server port (default 443, or 8000 if behind nginx reverse proxy)
# ACM2_PORT=443

# Optional: Server host (default 0.0.0.0)
# ACM2_HOST=0.0.0.0
